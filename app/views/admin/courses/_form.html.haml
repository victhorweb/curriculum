- if @course.errors.any?
  #error_explanation
    %h2
      = pluralize(@course.errors.count, "error")
      É proibido que esse curso seja salvo:
    %ul
      - @course.errors.full_messages.each do |msg|
        %li= msg
.field
  = f.label :name
  %br
  = f.text_field :name
.field
  = f.label :description
  %br
  = f.text_field :description
.actions
  = f.submit