- if @level.errors.any?
  #error_explanation
    %h2
      = pluralize(@level.errors.count, "error")
      Proibido salvar esse nível:
    %ul
      - @level.errors.full_messages.each do |msg|
        %li= msg
.field
  = f.label :name
  %br/
  = f.text_field :name
.field
  = f.label :score
  %br/
  = f.text_field :score
.actions
  = f.submit