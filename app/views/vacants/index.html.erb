<h1>Listing vacants</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Date end</th>
    <th>Sector</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @vacants.each do |vacant| %>
  <tr>
    <td><%= vacant.title %></td>
    <td><%= vacant.description %></td>
    <td><%= vacant.date_end %></td>
    <td><%= vacant.sector_id %></td>
    <td><%= link_to 'Show', vacant %></td>
    <td><%= link_to 'Edit', edit_vacant_path(vacant) %></td>
    <td><%= link_to 'Destroy', vacant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Vacant', new_vacant_path %>
